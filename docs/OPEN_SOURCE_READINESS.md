# Open-source repository readiness

## Scope

M11 prepared the repository's public entry points while it remained private. M12 executes public visibility, hosted CI, vulnerability reporting, and Cargo releases while retaining the same product and safety boundaries.

## Public entry-point contract

The root README must answer, in order:

1. What problem does Quatopsy solve?
2. Who is it for?
3. What can a new evaluator run immediately?
4. What does the output look like and who owns verdicts?
5. Which capabilities are shipped, and where are their safety boundaries?
6. How can someone build, test, contribute, get support, report a vulnerability, and understand the licence?

The canonical release lockup appears above the fold. The architecture image is generated by `scripts/brandkit.py`, recorded in `quatopsy.brand/2`, contains accessible title and description elements, uses no scripts or remote resources, and remains legible when scaled to a narrow README column.

## Community contract

- `CONTRIBUTING.md` defines the development gate, verdict and source-integrity invariants, protocol/rule evidence, adapter provenance, and pull-request expectations.
- `CODE_OF_CONDUCT.md` sets participation and private enforcement expectations.
- `SECURITY.md` routes vulnerabilities to GitHub private vulnerability reporting and warns against transmitting sensitive telemetry.
- `SUPPORT.md` separates normal issues from vulnerabilities and refuses mission-operations support expectations.
- Structured issue forms collect reproducible behavior, format semantics, provenance, and safety boundaries without inviting operational data.
- The pull-request template requires behavioral evidence, public-contract updates, data/licence review, and the authoritative local CI result.

## Repository metadata contract

`.github/repository-settings.json` is the reviewed source for GitHub About metadata. `scripts/check-community.py` validates its schema, description length, sorted topic syntax, public-visibility and hosted-CI contract, release registry, required community files, README local links, architecture accessibility, and SVG safety.

The approved description is:

> Find, explain, and visualize quaternion attitude failures, then evaluate controller and trajectory candidates locally.

Approved topics are `attitude-control`, `gnc`, `quaternion`, `robotics`, `rust`, `simulation`, `spacecraft`, `trajectory-optimization`, and `visualization`.

The canonical project site is `https://kabudu.github.io/quatopsy/`. It is a static presentation surface and does not provide hosted analysis. M12 records the owner's authorisation for public visibility, hosted CI, and crates.io release operations.

The description and nine topics were applied and read back from GitHub on 2026-08-27. Public visibility, hosted CI, and Cargo releases were subsequently authorised and executed under M12. The M13 site was deployed from reviewed `master` by Pages run `33287788648`; its live metadata and HTTPS response were verified before the canonical homepage URL was applied and read back from GitHub.

## Opening execution

M12 verifies the M11 files on `master`, changes visibility, immediately enables private vulnerability reporting and repository protections, enables the hosted form of the existing local gate, inspects the live public README and social preview, verifies issue forms and security links anonymously, and publishes the reviewed lockstep Cargo release. M13 adds the presentation-only project website. Signing, support commitments, hardware authority, and certification remain outside this opening.
